name: Cron Smoke Test

on:
  workflow_dispatch:
  schedule:
    - cron: "3-58/5 * * * *"

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Show scheduled time
        run: |
          echo "Cron smoke test started"
          date -u
